[pytest]
# Run with: pytest tests/ (all), or pytest -m "not external" (skip connectivity checks when no secrets)
markers =
    external: marks tests that call external APIs (Pinecone, LLM, Tavily, Mermaid.ink); skip when env vars not set
